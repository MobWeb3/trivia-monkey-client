export interface MetaphorEntry {
    author: string | null;
    extract: string | null;
    id: string;
    published_date: string | null;
    score: number | null;
    title: string;
    url: string;
  }
